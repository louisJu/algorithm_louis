
import programmers.Trytrytry

fun main(args: Array<String>) {
    println("Hello World!")
    // Solution().rotate(arrayOf(1, 2, 3, 4, 5, 6, 7), 3)
    /*val iterator: Iterator<Any> = iterator {
        yield(1)
        yield("hello")
        yield(10L)
    }
    println(iterator.next())
    println(iterator.next())
    println(iterator.next())*/

    // println(task1().solution(intArrayOf(9,3,9,3,7)))

    //println(Trytrytry().solution(5))

/*val iterator2: Iterator<Int> = iterator {
        yield(1)
        yield("hello")
        yield(10L)
    }*/ //
}
